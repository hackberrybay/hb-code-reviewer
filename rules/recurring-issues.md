# Recurring Issues Log

Auto-managed by `/review-pr` and `/improve-rules`. Do not edit manually.

Issues are appended here by `/review-pr` when a pattern appears 3+ times in a single run.
They are promoted into permanent rule files by `/improve-rules` when the occurrence count reaches 3 across sessions.

**Categories:** `typescript` | `security`

**Format:**
```
## {ISO date} | {category} | {issue-key}
{one-line description of the violation pattern}
Occurrences: {n}
---
```

**Promotion:** When `Occurrences >= 3`, run `/improve-rules` to move the rule into `rules/typescript.md` or `rules/security.md`.

<!-- entries appended below this line -->
